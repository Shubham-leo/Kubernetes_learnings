apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Use the base as starting point
resources:
  - ../../base

# Deploy to dev namespace
namespace: dev

# Add dev-specific labels
commonLabels:
  environment: dev

# Apply patches
patches:
  - path: replica-patch.yaml
